services:
  - type: web
    name: sistema-pdv-backend
    env: node
    plan: free
    buildCommand: cd backend && npm install && npm rebuild better-sqlite3 && npm run build
    startCommand: node backend/dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
